
GROQ_API_KEY="..."

# DB
POSTGRES_USER=root
POSTGRES_PASSWORD=rootpass
POSTGRES_DB=trafficlawdb
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

DATABASE_URL="postgresql+asyncpg://[user]:[pass]@localhost:[port]/[dbname]"

DATA_RAW_PATH="[pathname]"
DATA_PROCESSED_PATH="[pathname]"
EVAL_TESTSET_PATH="[pathname]"

# Next.js Frontend Configuration (frontend/web/.env.local)
# For production: Set to your API Gateway URL
# NEXT_PUBLIC_API_URL="http://localhost:3001"
# IMPORTANT: Use 127.0.0.1 instead of localhost for local development

# Next.js API Gateway Configuration (frontend/api/.env.local)
# For production: Set to your Python backend URL
# IMPORTANT: Use 127.0.0.1 instead of localhost to match Python backend host
PYTHON_API_URL="http://127.0.0.1:8000"

# Security Configuration (CRITICAL FOR PRODUCTION)
# Comma-separated list of allowed origins for CORS
# 
# For development: Leave commented (defaults to localhost)
# For production: Set to your actual domain(s) - REQUIRED!
# 
# Set this in BOTH:
#   1. Next.js API Gateway (frontend/api/.env.local or platform env vars)
#   2. Python Backend (.env or platform env vars)
#
# Example for production:
# ALLOWED_ORIGINS="https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com"
#
# Development (optional - already default):
# ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001"