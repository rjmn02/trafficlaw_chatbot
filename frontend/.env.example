# Dev: leave unset — Vite proxies /api/* to Python at http://127.0.0.1:8000
# Prod (Vercel): set before build — your Render backend URL, no trailing slash
# VITE_API_URL=https://your-service.onrender.com
# APP API KEY